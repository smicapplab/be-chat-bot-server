{
  "name": "be-chat-bot-schema",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "migrate:create": "db-migrate create --sql-file --env dev --config database.config.js",
    "migrate:dev": "db-migrate up --env dev --config database.config.js",
    "migrate:prod": "db-migrate up --env prod --config database.config.js",
    "rollback:dev": "db-migrate down --env dev --config database.config.js",
    "rollback:prod": "db-migrate down --env prod --config database.config.js"
  },
  "devDependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2",
    "node-pg-migrate": "^7.9.1"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "pg": "^8.14.1"
  }
}